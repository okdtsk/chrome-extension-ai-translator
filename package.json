{
  "name": "chrome-extension-ai-translator",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that translates selected text via OpenAI, Gemini, Claude, or Ollama.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
